import { Text } from "react-native";

export default function HomeScreen() {
  return (
    <Text  style={{display: "flex", height: "100%", alignItems: "center", justifyContent: "center", backgroundColor: "lavender"}}>T-PLANNER</Text>
  );
}
